# 当前已完成功能说明

更新时间：2026-05-28

## 1. 项目总体状态

当前项目已经完成了“可运行的前后端基础平台”搭建，并落地了以下核心能力：

- 前后端基础工程已初始化完成
- 登录认证链路已打通
- 账号与角色管理已具备可用的前后端实现
- 基础数据模块已具备较完整的增删改查能力
- 毕业要求与指标点模块已具备较完整的增删改查能力
- 开发环境与服务器部署链路已打通

同时，项目中仍有部分模块目前处于“导航占位 / 页面骨架”阶段，尚未补齐真实业务表单、导入导出和计算逻辑。

## 2. 已完成的基础能力

### 2.1 前端基础能力

- Vue 3 前端工程已搭建完成
- 路由、状态管理、请求封装已接入
- 登录页、主布局页、首页、404 页已具备可访问页面
- 基于角色的导航菜单与受保护路由已接入
- 前端请求已统一通过 `/api` 前缀与后端联调

### 2.2 后端基础能力

- Spring Boot 后端工程已搭建完成
- 统一返回体、全局异常处理已接入
- JWT 登录认证与鉴权拦截已接入
- 健康检查接口 `/health` 已可访问
- Swagger / OpenAPI 相关配置已接入

### 2.3 数据库基础能力

- MySQL 独立部署方案已接入
- 当前数据库已完成核心表结构落库
- 账号、角色、权限、基础数据、毕业要求相关表已存在
- 项目当前数据库中已存在视图、审计日志表、配置表等扩展对象

## 3. 已完成的业务功能

### 3.1 登录认证

已完成：

- 登录接口
- 用户信息获取接口
- 基于 JWT 的登录态校验
- 不同业务角色的基础权限承载

当前可支撑：

- 用户登录
- 登录后获取用户基本信息、角色和权限
- 前端根据登录态进行页面访问控制

### 3.2 账号与角色管理

已完成：

- 账号列表查询
- 可分配角色列表查询
- 新增账号
- 编辑账号
- 启用 / 禁用账号
- 角色分配

对应能力已经覆盖前后端页面、接口与数据库联动，属于当前项目中较完整的系统管理模块。

### 3.3 基础数据模块

基础数据模块已完成真实页面与接口联调，不再只是占位页。

已完成的子模块包括：

- 学年学期管理
- 学院管理
- 专业管理
- 课程管理

其中已具备的典型能力包括：

- 列表查询
- 条件筛选
- 新增
- 编辑
- 启用 / 停用
- 删除

### 3.4 毕业要求与指标点模块

已完成真实页面与接口联调。

已完成：

- 毕业要求列表查询
- 毕业要求新增、编辑、删除
- 指标点列表查询
- 指标点新增、编辑、删除
- 与专业数据的联动加载

该模块已经可以支撑专业负责人进行基础配置维护。

## 4. 已完成的部署与运行能力

### 4.1 开发与部署基础设施

已完成：

- GitHub Actions CI / CD 基础流程
- 前端构建流程
- 后端构建流程
- 开发服务器自动部署流程
- Nginx 反向代理
- 前端静态资源部署
- 后端运行与健康检查

### 4.2 当前部署形态

当前项目部署方式为：

- 前端通过 Docker 构建并由容器提供静态页面
- Nginx 作为统一入口，对外暴露 `80` 端口
- 后端以 `app.jar` 形式在服务器本机运行
- 数据库采用服务器独立 MySQL
- GitHub Actions 可将目标分支代码构建并部署到服务器

### 4.3 当前已验证能力

已验证：

- `/health` 可访问
- 登录接口可用
- 前后端联调链路可用
- MySQL 已接入后端运行环境
- Nginx `/api` 代理链路已打通

## 5. 当前仍为占位或待补全的模块

以下模块在导航结构中已经存在，但当前仍主要是骨架页面或模块边界占位，尚未完成完整业务实现：

- 支撑矩阵配置
- 课程目标与考核点
- 成绩录入与计算
- 报表与底稿

这些页面当前主要用于承接：

- 模块标题
- 模块职责说明
- 对应核心实体说明

但尚未补齐：

- 真实业务表单
- 完整数据表格
- 导入导出功能
- 计算流程
- 报表生成逻辑

