/** 路由名称（前端2 新增路由时请保持名称一致） */
export const ROUTE_NAMES = {
  LOGIN: 'Login',
  /** 后台首页，由 zml 在 router 中注册 */
  HOME: 'Home',
  NOT_FOUND: 'NotFound',
}

/** 登录后默认跳转路径（与前端2 首页 path 对齐） */
export const DEFAULT_HOME_PATH = '/home'

export const TOKEN_KEY = 'edu_admin_token'
