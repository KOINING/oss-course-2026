# SQL

本目录用于存放数据库相关交付物。

当前阶段建议统一维护以下内容：

- ER 图
- 建表 SQL
- 初始化脚本
- 数据字典

## 当前约定

- 先保留 `sql/` 目录和说明文件，不预生成具体 SQL 模板。
- 等 ER 图和核心表结构确定后，再按实际需要补充建库、建表和初始化脚本。
- SQL 文件名建议采用有顺序的前缀，便于服务器执行和后续维护。

## 协作约定

- SQL 文件在本地编写，并提交到仓库统一管理。
- 服务器上的 MySQL 通过执行本目录中的 SQL 文件落地数据库结构。
- 不建议只在服务器上临时手工写 DDL。
